{% extends 'base.html.twig' %}

{% block title %}FotoGaleria index{% endblock %}

{% block body %}
    <h1>FotoGaleria index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre_foto</th>
                <th>IdGaleria</th>
                <th>Url_foto</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for foto_galerium in foto_galerias %}
            <tr>
                <td>{{ foto_galerium.id }}</td>
                <td>{{ foto_galerium.nombreFoto }}</td>
                <td>{{ foto_galerium.idGaleria }}</td>
                <td>{{ foto_galerium.urlFoto }}</td>
                <td>
                    <a href="{{ path('foto_galeria_show', {'id': foto_galerium.id}) }}">show</a>
                    <a href="{{ path('foto_galeria_edit', {'id': foto_galerium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('foto_galeria_new') }}">Create new</a>
{% endblock %}
