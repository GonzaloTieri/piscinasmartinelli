<form method="post" action="{{ path('foto_galeria_delete', {'id': foto_galerium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ foto_galerium.id) }}">
    <button class="btn">Delete</button>
</form>
